scalar Date

type User {
    id: ID!,
    firstName: String!,
    lastName: String!,
    email: String!,
    mobilePhone: String!,
    gender: String!,
    birthDate: Date!,
    userTag: String!,
    username: String!,
    password: String!,
    role: String!,
    verified: Boolean!
}

type AuthPayload {
    username: String,
    email: String,
    token: String!
}